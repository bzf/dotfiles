# vim: ft=muttrc

set from = "hello@bzf.se"
set mbox = "+fastmail/archive"
set postponed = "+fastmail/drafts"
set sendmail = "/usr/local/bin/msmtp -a fastmail"

macro index A "<save-message>=fastmail/archive<enter><enter>" "Archive Message"
